package tokens;

/**
 * Creates a token that stores a number in it
 * @author Robert Dallara
 *
 */
public class ANumberToken extends NumberToken {

	/**
	 * Creates a token that stores a number
	 * @param number the number to store in the token
	 */
	public ANumberToken(String number)
	{
		super(number);
	}
}
