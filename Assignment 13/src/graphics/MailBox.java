package graphics;

import basicgeometry.*;

/**
 * An interface used as a type for mail box objects
 * @author Robert Dallara
 *
 */
public interface MailBox extends LeggedObject {

	void setLocation(Point newLocation);
}
